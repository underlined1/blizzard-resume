/*
 * This file is safe to publish: Supabase publishable/anon keys are designed
 * for browser use and are protected by the row-level security rules in
 * supabase/schema.sql. Never put a secret or service_role key here.
 */
window.SUPABASE_CONFIG = Object.freeze({
  url: "",
  publishableKey: ""
});
